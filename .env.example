DB_PASSWORD=your-strong-database-password
JWT_KEY=your-jwt-secret-must-be-at-least-32-characters-long
ADMIN_PASSWORD=your-admin-password

# Set automatically by GitHub Actions — only needed if running Docker locally
API_IMAGE=ghcr.io/your-github-username/wristwise-api:latest
